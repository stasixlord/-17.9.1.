array = [int(x) for x in input("Введите числа от 1 до 999 в любом порядке, через пробел: ").split()]

def merge_sort(массив): 
 если len(массив) < 2: 
 return array[:] # выход из рекурсии
 ещё:
 средний = len(массив) // 2 
 слева = merge_sort(массив[:средний]) 
 right = merge_sort(массив[средний:]) 
 return merge(left, right) # выполняем слияние


def слияние (слева, справа):
 результат = []
 i, j = 0, 0

 в то время как i < len(слева) и j < len(справа):
 если слева[i] < справа [j]:
 результат.добавить(слева[i])
 i += 1
 ещё:
 результат.добавить(справа[j])
 j += 1

 в то время как i < len(слева):
 результат.добавить(слева[i])
 i += 1

 в то время как j < len(справа):
 результат.добавить(справа[j])
 j += 1

 возвращает результат

печать(merge_sort(массив))


определить двоичный поиск (массив, элемент, слева, справа):
 если слева> справа: 
 возвращает False 

 middle = (right + left) // 2 # находимо середину
 если массив[средний] == элемент: 
 return middle # возвращаем этот индекс
 элемент elif <массив[средний]: 

 возвращает двоичный поиск (массив, элемент, левый, средний - 1)
 ещё: 
 возвращает двоичный поиск (массив, элемент, середина + 1, справа)


пока True:
 попробуйте:
 element = int(input("Введите число от 1 до 999: "))
 если элемент <0 или элемент > 999:
 вызвать исключение
 перерыв
 кроме ошибки значения:
 print("Нужно ввести число!")
 за исключением исключения:
 print("Неправильный диапазон!")


print(двоичный поиск(массив, элемент, 0, len(массив)))
