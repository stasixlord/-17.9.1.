array = [int(x) for x in input("Введите числа от 1 до 999 в любом порядке, через пробел: ").split()]

def merge_sort(массив): 
 если len(массив) < 2: 
 возвращает массив[:] 
 ещё:
 средний = len(массив) // 2 
 left = merge_sort(массив[:средний]) 
 right = merge_sort(массив[средний:]) 
 обратное слияние (слева, справа) 


def слияние (слева, справа):
 результат = []
 i, j = 0, 0

    while i < len(left) and j < len(right):
        if left[i] < right[j]:
            result.append(left[i])
            i += 1
        else:
            result.append(right[j])
            j += 1

    while i < len(left):
        result.append(left[i])
        i += 1

    while j < len(right):
        result.append(right[j])
        j += 1

    return result

печать(merge_sort(массив))


определить двоичный поиск (массив, элемент, слева, справа):
 если влево > вправо: 
 возвращает значение False 

 средний = (правый + левый) // 2 
 если массив[средний] == элемент: 
 возвращение в середину 
 элемент elif < массив[средний]: 
    
 возвращает binary_search(массив, элемент, левый, средний - 1)
 ещё: 
 возвращает binary_search(массив, элемент, середина + 1, справа)


в то время Как True:
 попробуй:
 element = int(input("Введите число от 1 до 999: "))
 если элемент < 0 или элемент > 999:
 вызвать исключение
 перерыв
 кроме ValueError:
 print("Нужно ввести число!")
 за исключением исключения:
 print("Неправильный диапазон!")


print(binary_search(массив, элемент, 0, len(массив)))
